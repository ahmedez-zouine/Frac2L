#include "fractol.h"

int main(int argc, char *argv[])
{
  int i;
  
  if (argc == 2 && ft_strcmp(argc[1], "Mandelbrot")
  {
    ft_mlxinit(&fract);
    ft_mlxrender(&fract);
    ft_mlxloop(
    // Mandelbrot
  } else if (argc == 4 && ft_strcmp(argv[1], "Julia")
  {
    // Julia
  }
  else
  {
    // Message Error
    exit(EXIT_FAILURE);
  }

  return (0);
}
